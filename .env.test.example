# Copy to .env.test on the server. Never commit the real file.
POSTGRES_PASSWORD=replace-with-a-long-random-password
JWT_SECRET=replace-with-at-least-32-random-bytes
APP_ORIGIN=http://107.150.99.108:8080
APP_PORT=8080

# Keep seed data enabled only for an isolated test environment.
SEED_DEMO_DATA=true

# Start with explicit Mock until Ollama is installed/reachable on the server.
AI_MODE=mock
AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
OLLAMA_MODEL=qwen2.5:7b
AI_DAILY_BUDGET_USD=1.0
